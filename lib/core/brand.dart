import 'package:flutter/material.dart';

class Brand {
  // Color seeds
  static const Color primarySeed = Color(0xFF0B57D0);
  static const Color secondarySeed = Color(0xFF006A6A);

  // Spacing scale (4pt grid)
  static const double spaceXS = 4;
  static const double spaceS = 8;
  static const double spaceM = 16;
  static const double spaceL = 24;
  static const double spaceXL = 32;

  // Radius
  static const double radiusS = 8;
  static const double radiusM = 12;
  static const double radiusL = 16;

  // Motion
  static const Duration motionFast = Duration(milliseconds: 120);
  static const Duration motionBase = Duration(milliseconds: 200);
  static const Duration motionSlow = Duration(milliseconds: 300);
}
